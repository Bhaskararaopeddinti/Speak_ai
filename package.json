{
  "name": "linguavoice-ai",
  "version": "1.0.0",
  "private": true,
  "description": "LinguaVoice AI - Modern AI Voice Language Tutor",
  "scripts": {
    "build": "npm --prefix client install --include=dev && npm --prefix client run build && npm --prefix server install --include=dev && npm --prefix server run build",
    "start": "npm --prefix server run start",
    "install:all": "npm --prefix server install --include=dev && npm --prefix client install --include=dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build:client": "npm --prefix client run build",
    "build:server": "npm --prefix server run build"
  },
  "keywords": [
    "ai",
    "language-learning",
    "whisper",
    "tts",
    "gpt-4",
    "react",
    "tailwindcss"
  ],
  "author": "",
  "license": "MIT"
}
